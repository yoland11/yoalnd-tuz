# Core runtime
NODE_ENV=development
LOG_LEVEL=info

# API server
PORT=8080
DATABASE_URL=DATABASE_URL=postgresql://postgres:hussen1998AA@db.lmchfjycehhwdqnprhxn.supabase.co:5432/postgres
PUBLIC_BASE_URL=https://yoland-alijan.vercel.app/

# Frontend / Vite
# Use PORT=20796 and BASE_PATH=/ when running the frontend locally.
BASE_PATH=/

# First admin seed
# In production, set these before the first API boot. The fallback password is
# allowed only outside production.
ADMIN_USERNAME=alijan
ADMIN_PASSWORD=change-this-before-production
ADMIN_FULL_NAME=المدير الرئيسي

# WhatsApp providers. Configure one provider, then enable events in the admin UI.
ULTRAMSG_INSTANCE_ID=
ULTRAMSG_TOKEN=
WASSENGER_API_KEY=
WASSENGER_DEVICE=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+10000000000
META_WA_PHONE_ID=
META_WA_TOKEN=
